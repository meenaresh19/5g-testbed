apiVersion: v1
kind: Service
metadata:
  name: mongodb
  namespace: 5g-testbed
  labels:
    app: mongodb
spec:
  clusterIP: None  # Headless service for StatefulSet
  selector:
    app: mongodb
  ports:
    - name: mongodb
      port: 27017
      targetPort: 27017
      protocol: TCP
